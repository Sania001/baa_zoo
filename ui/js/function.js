/**
 * Created with JetBrains PhpStorm.
 * User: ICap
 * Date: 25.11.13
 * Time: 13:28
 * To change this template use File | Settings | File Templates.
 */
function getId(id)
{
    return document.getElementById(id);
}
/*
function work(obj,w) {
    $.post(w.action,
        {
            name_r: "za", /*getId('name_r').value*///,
  /*          fam_r: getId('fam_r').value.toString(),
            otch: getId('otch_r').value,
            login_r: getId('login_r').value,
            pass_r: getId('pass_r').value,
            checkpass_r: getId('checkpass_r').value,
            cap_r: getId('cap_r').value,
            role_r: getId('role_r').value,// Передаем идентивикатор запроса
            data: obj.id        // Передаем идентивикатор элемента
        },
        function (data) {
        //  $(obj).html(data)   // Получаем данные после обработки
    });

}
*/